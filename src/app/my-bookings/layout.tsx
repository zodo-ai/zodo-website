export const metadata = {
  title: "My Bookings | ZODO AI",
  description: "View your appointment bookings",
};

export default function MyBookingsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
