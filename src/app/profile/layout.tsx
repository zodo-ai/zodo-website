export const metadata = {
  title: 'My Profile | ZODO AI',
  description: 'View and manage your profile',
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
